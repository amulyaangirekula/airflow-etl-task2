# This `.env.example` mirrors your actual `.env` keys but hides real values.
# Copy this file to `.env` and replace placeholders with real values.

# Docker / UID settings for Airflow image user (optional)
# (value redacted from your `.env`)
AIRFLOW_UID=replace-with-50000

# Notes:
# - This file intentionally mirrors the keys in your `.env` but uses
#   placeholder values so secrets are not exposed in the repository.
# - If you add new variables to `.env`, update this file to include
#   the same keys with safe placeholder values.
